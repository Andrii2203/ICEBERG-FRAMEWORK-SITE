The Only Valid Set of Restrictions for Claude During UI Audit Generation
Цей документ визначає жорсткі заборони, які Claude повинен дотримуватися під час генерації 6 файлів UI‑аудиту.
Це абсолютні правила.
Вони не обговорюються, не змінюються і не ігноруються.

Claude повинен виконувати тільки ICEBERG EXECUTION MAP — CARD 4 та ICEBERG AUDIT FILE STANDARD.
Все інше — заборонено.

1. GLOBAL PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

вигадувати UI‑елементи, яких немає на зображенні

додавати нові файли

видаляти файли

змінювати назви файлів

змінювати структуру файлів

змінювати формат JSON

додавати нові поля в JSON

видаляти поля з JSON

змінювати типи даних у JSON

генерувати будь‑які інші артефакти, крім 6 файлів

2. CONTENT PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

вигадувати функції, яких не видно

вигадувати сторінки, яких не видно

вигадувати компоненти, яких не видно

вигадувати бізнес‑логіку

вигадувати flows, яких не видно

вигадувати дані, яких не видно

вигадувати текст, якого не видно

вигадувати стани, яких не видно

Claude описує тільки те, що реально видно на UI‑зображенні.

3. SCOPE PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

згадувати будь‑які внутрішні системи

Claude працює всередині одного завдання:
аналіз UI → генерація 6 файлів.

4. PROCESS PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

змінювати порядок файлів

змінювати логіку генерації

змінювати структуру секцій

змінювати назви секцій

додавати нові секції

видаляти секції

переставляти секції

змінювати формат Markdown

змінювати формат JSON

5. OUTPUT PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

повертати менше ніж 6 файлів

повертати більше ніж 6 файлів

повертати файли в іншому форматі

повертати файли в іншому порядку

повертати текст поза файлами

повертати пояснення

повертати коментарі

повертати мета‑інформацію

повертати “думки”, reasoning, chain‑of‑thought

Claude повертає тільки 6 файлів, нічого більше.

6. STYLE PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

використовувати художній стиль

використовувати емоційні оцінки

використовувати метафори

використовувати вигадані сценарії

використовувати “AI‑мову”

використовувати disclaimers

використовувати фрази “як AI”, “як модель”

Стиль повинен бути:

професійний

структурований

нейтральний

точний

без води

7. SELF‑CHECK PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

пропускати self‑check

повертати результат без перевірки

ігнорувати невідповідності

залишати помилки в JSON

залишати порушення структури

8. MENTAL MODEL PROHIBITIONS
Claude НЕ МАЄ ПРАВА:

виходити за межі задачі

робити припущення

додавати контекст

змінювати правила

змінювати стандарти

змінювати карту

змінювати формат

Claude працює строго в межах:

Execution Map — Card 4

Audit File Standard

Audit Prohibitions

9. END OF PROHIBITIONS
Це повний, жорсткий, мінімалістичний набір заборон, який гарантує:

детермінованість

відсутність хаосу

стабільність

передбачуваність

якість

повторюваність

Claude працює тільки в цих рамках.